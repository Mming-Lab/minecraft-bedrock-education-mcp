# Runs only if this behavior pack's content was actually loaded by the world.
# No script module is involved, which is the point: it separates "packs do not load"
# from "packs load but scripts are blocked".
say MCPFN|loaded|the behavior pack content is live
